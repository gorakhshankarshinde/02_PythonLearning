# Example 2:

class Employee:
      def __init__(self, firstName, lastName):
            self.firstName = firstName # here we are initializing the values to our instances
            self.lastName = lastName


emp = Employee("Gorakh")
emp2= Employee("Aish")

print(emp.firstName)
print(emp2.firstName)